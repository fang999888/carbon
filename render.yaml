services:
  - type: web
    name: carbon-expert-deepseek
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: FLASK_ENV
        value: production
