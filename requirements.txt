# 後端依賴項 (用于 Render 部署 - 支援 DeepSeek API)
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0
requests==2.31.0
openai==1.3.0  # DeepSeek 使用 OpenAI 兼容 SDK

# 資料庫相關 (如果需要)
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9
# pymongo==4.5.0  # 如果使用 MongoDB
